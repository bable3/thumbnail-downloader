import { DateFromTodayPipe } from './date-from-today.pipe';

describe('DateFromTodayPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFromTodayPipe();
    expect(pipe).toBeTruthy();
  });
});
