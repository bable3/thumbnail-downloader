import { NumberProcessPipe } from './number-process.pipe';

describe('NumberProcessPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberProcessPipe();
    expect(pipe).toBeTruthy();
  });
});
