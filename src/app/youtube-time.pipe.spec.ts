import { YoutubeTimePipe } from './youtube-time.pipe';

describe('YoutubeTimePipe', () => {
  it('create an instance', () => {
    const pipe = new YoutubeTimePipe();
    expect(pipe).toBeTruthy();
  });
});
